/** OBSIDIAN v4.0 — config/build-info.js · human-readable build marker.
 *  Shown in the app footer so the running browser build can be confirmed at a glance (resolves
 *  "am I looking at the latest code or a stale service-worker cache?"). Bump `id` on each pushed build. */
export const BUILD_INFO = { id: 'U6d-20260610', date: '2026-06-10' };
export default BUILD_INFO;
